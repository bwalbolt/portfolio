import { RoutePlaceholder } from "../_components/route-placeholder";

export default function AboutPage() {
  return (
    <RoutePlaceholder
      description="The dedicated about page will extend the homepage overview with process, collaborations, and experience highlights."
      eyebrow="Profile"
      title="About"
    />
  );
}
