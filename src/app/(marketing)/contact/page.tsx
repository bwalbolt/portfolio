import { RoutePlaceholder } from "../_components/route-placeholder";

export default function ContactPage() {
  return (
    <RoutePlaceholder
      description="The full contact workflow will move here once the serverless email endpoint is implemented."
      eyebrow="Direct Line"
      title="Contact"
    />
  );
}
