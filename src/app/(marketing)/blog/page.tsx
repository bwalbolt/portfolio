import { RoutePlaceholder } from "../_components/route-placeholder";

export default function BlogPage() {
  return (
    <RoutePlaceholder
      description="Long-form writing, process notes, and implementation details will live here once the content system lands."
      eyebrow="Knowledge Base"
      title="Blog"
    />
  );
}
