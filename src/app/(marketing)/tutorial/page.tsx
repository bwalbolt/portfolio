import { RoutePlaceholder } from "../_components/route-placeholder";

export default function TutorialPage() {
  return (
    <RoutePlaceholder
      description="Tutorial posts will be filtered views over the blog collection, with production-ready examples and copyable code."
      eyebrow="Tutorial Filter"
      title="Tutorials"
    />
  );
}
